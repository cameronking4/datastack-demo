---
title: Get workspace settings
description: Get workspace-level settings (defaults, features, retention)
---

`GET /api/v1/workspaces/{workspaceId}/settings`

Get workspace-level settings (defaults, features, retention)

## Path Parameters

| Parameter | Type | Required | Description |
|---|---|---|---|
| `workspaceId` | `string` | Yes | The workspaceId value. |

## Response `200`

Success

## Example

```bash title="cURL"
curl -X GET "/api/v1/workspaces/{workspaceId}/settings" \
  -H "Authorization: Bearer <accessToken>"
```
