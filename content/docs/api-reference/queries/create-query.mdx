---
title: Create query
description: Save a new SQL query with catalog/schema context and result format preferences
---

`POST /api/v1/queries`

Save a new SQL query with catalog/schema context and result format preferences

## Request Body

| Field | Type | Required | Description |
|---|---|---|---|
| `name` | `string` | Yes | The name value. |
| `description` | `string` | No | The description value. |
| `warehouseId` | `string` | Yes | The warehouseId value. |
| `queryText` | `string` | Yes | The queryText value. |
| `catalog` | `string` | No | The catalog value. |
| `schema` | `string` | No | The schema value. |
| `resultFormat` | `string` | No | The resultFormat value. |
| `timeout` | `integer` | No | The timeout value. |
| `parameters` | `array<object>` | No | The parameters value. |
| `tags` | `array<string>` | No | The tags value. |

## Response `201`

Created

## Example

```bash title="cURL"
curl -X POST "/api/v1/queries" \
  -H "Authorization: Bearer <accessToken>" \
  -H "Content-Type: application/json" \
  -d '{
  "name": "example-name",
  "description": "example-description",
  "warehouseId": "example-warehouseId",
  "queryText": "example-queryText",
  "catalog": "example-catalog",
  "schema": "example-schema",
  "resultFormat": "example-resultFormat",
  "timeout": 10,
  "parameters": [],
  "tags": []
}'
```
