---
title: Rollback Deployment
description: Rollback to a previous deployment version.
---

```
POST /api/v1/deployments/{deploymentId}/rollback
```

Rolls back the stack to the state of a previous deployment.

**Required scope:** `manage:deployments`

## Path Parameters

| Parameter | Type | Description |
|---|---|---|
| `deploymentId` | `string` | Deployment identifier to rollback from. |

## Response `202`

| Field | Type | Description |
|---|---|---|
| `id` | `string` | New rollback deployment identifier. |
| `stackId` | `string` | Stack identifier. |
| `stackName` | `string` | Stack display name. |
| `version` | `number` | Version being rolled back to. |
| `environment` | `string` | Target environment. |
| `status` | `string` | Status (`IN_PROGRESS`). |
| `rollbackFrom` | `string` | Deployment being rolled back. |
| `rollbackTo` | `string` | Target deployment version. |
| `steps` | `object[]` | Per-layer rollback steps. |
| `startedAt` | `string` | ISO 8601 start timestamp. |
| `startedBy` | `string` | Initiator identity. |

## Example

```bash title="cURL"
curl -X POST "/api/v1/deployments/deploy-003/rollback" \
  -H "Authorization: Bearer <accessToken>"
```

```json title="Response"
{
  "id": "deploy-rollback",
  "stackId": "stack-001",
  "stackName": "Analytics Platform",
  "version": 2,
  "environment": "PRODUCTION",
  "status": "IN_PROGRESS",
  "rollbackFrom": "deploy-003",
  "rollbackTo": "deploy-002",
  "steps": [
    { "layer": "ingestion", "resourceType": "pipeline", "resourceId": "pipe-001", "action": "ROLLBACK", "status": "PENDING" },
    { "layer": "compute", "resourceType": "cluster", "resourceId": "cluster-001", "action": "NO_CHANGE", "status": "SKIPPED" }
  ],
  "startedAt": "2024-06-10T12:00:00Z",
  "startedBy": "api"
}
```
