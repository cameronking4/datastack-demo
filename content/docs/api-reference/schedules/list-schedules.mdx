---
title: List schedules
description: List unified schedules across jobs, pipelines, and maintenance windows
---

`GET /api/v1/schedules`

List unified schedules across jobs, pipelines, and maintenance windows

## Query Parameters

| Parameter | Type | Required | Description |
|---|---|---|---|
| `resourceType` | `string` | No | The resourceType value. |
| `active` | `boolean` | No | The active value. |

## Response `200`

Success

## Example

```bash title="cURL"
curl -X GET "/api/v1/schedules" \
  -H "Authorization: Bearer <accessToken>"
```
