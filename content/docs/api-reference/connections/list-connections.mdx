---
title: List connections
description: List external data source and sink connections with health status
---

`GET /api/v1/connections`

List external data source and sink connections with health status

## Query Parameters

| Parameter | Type | Required | Description |
|---|---|---|---|
| `type` | `string` | No | The type value. |
| `status` | `string` | No | The status value. |

## Response `200`

Success

## Example

```bash title="cURL"
curl -X GET "/api/v1/connections" \
  -H "Authorization: Bearer <accessToken>"
```
