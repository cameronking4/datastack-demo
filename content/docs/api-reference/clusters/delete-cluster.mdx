---
title: Terminate Cluster
description: Permanently terminate a cluster and release all resources.
---

```
DELETE /api/v1/clusters/{clusterId}
```

Permanently terminate a cluster and release all resources.

**Required scope:** `manage:clusters`

## Path Parameters

| Parameter | Type | Description |
|---|---|---|
| `clusterId` | `string` | The cluster identifier. |

## Response `200`

| Field | Type | Description |
|---|---|---|
| `message` | `string` | Confirmation message. |

## Example

```bash title="cURL"
curl -X DELETE /api/v1/clusters/cluster-001 \
  -H "Authorization: Bearer <accessToken>"
```

```json title="Response"
{
  "message": "Cluster cluster-001 termination initiated."
}
```
