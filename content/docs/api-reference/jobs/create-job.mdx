---
title: Create job
description: Create a new scheduled or triggered job
---

`POST /api/v1/jobs`

Create a new scheduled or triggered job

## Request Body

| Field | Type | Required | Description |
|---|---|---|---|
| `name` | `string` | Yes | The name value. |
| `description` | `string` | No | The description value. |
| `schedule` | `string` | No | The schedule value. |
| `timezone` | `string` | No | The timezone value. |
| `triggerType` | `string` | Yes | The triggerType value. |
| `maxConcurrentRuns` | `integer` | No | The maxConcurrentRuns value. |
| `retryPolicy` | `object` | No | The retryPolicy value. |
| `alertDestinations` | `array<object>` | No | The alertDestinations value. |
| `settings` | `object` | Yes | The settings value. |
| `tags` | `object` | No | The tags value. |

## Response `201`

Created

## Example

```bash title="cURL"
curl -X POST "/api/v1/jobs" \
  -H "Authorization: Bearer <accessToken>" \
  -H "Content-Type: application/json" \
  -d '{
  "name": "example-name",
  "description": "example-description",
  "schedule": "example-schedule",
  "timezone": "example-timezone",
  "triggerType": "example-triggerType",
  "maxConcurrentRuns": 10,
  "retryPolicy": {},
  "alertDestinations": [],
  "settings": {},
  "tags": {}
}'
```
