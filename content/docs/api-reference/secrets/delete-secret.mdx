---
title: Delete Secret
description: Delete a secret from a scope.
---

```
DELETE /api/v1/secrets
```

Deletes a single secret from a scope.

**Required scope:** `manage:secrets`

## Request Body

| Field | Type | Required | Description |
|---|---|---|---|
| `scope` | `string` | Yes | Scope name. |
| `key` | `string` | Yes | Secret key to delete. |

## Response `200`

| Field | Type | Description |
|---|---|---|
| `message` | `string` | Confirmation message. |

## Example

```bash title="cURL"
curl -X DELETE "/api/v1/secrets" \
  -H "Authorization: Bearer <accessToken>" \
  -H "Content-Type: application/json" \
  -d '{ "scope": "aws-prod", "key": "session_token" }'
```

```json title="Response"
{
  "message": "Secret 'session_token' deleted from scope 'aws-prod'."
}
```
