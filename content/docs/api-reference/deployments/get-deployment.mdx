---
title: Get Deployment
description: Get deployment details including per-layer status.
---

```
GET /api/v1/deployments/{deploymentId}
```

Returns detailed deployment information including step-by-step layer status.

**Required scope:** `read:deployments`

## Path Parameters

| Parameter | Type | Description |
|---|---|---|
| `deploymentId` | `string` | Deployment identifier. |

## Response `200`

| Field | Type | Description |
|---|---|---|
| `id` | `string` | Deployment identifier. |
| `stackId` | `string` | Stack identifier. |
| `stackName` | `string` | Stack display name. |
| `version` | `number` | Stack version deployed. |
| `environment` | `string` | Target environment. |
| `status` | `string` | Overall status. |
| `steps` | `object[]` | Per-layer deployment steps. |
| `steps[].layer` | `string` | Layer name. |
| `steps[].resourceType` | `string` | Resource type. |
| `steps[].resourceId` | `string` | Resource identifier. |
| `steps[].action` | `string` | Action taken (`UPDATE`, `CREATE`, `NO_CHANGE`, `ROLLBACK`). |
| `steps[].status` | `string` | Step status (`SUCCEEDED`, `FAILED`, `SKIPPED`, `PENDING`). |
| `steps[].startedAt` | `string` | ISO 8601 start timestamp (null if skipped). |
| `steps[].completedAt` | `string` | ISO 8601 completion timestamp (null if skipped). |
| `steps[].logs` | `string` | Step log message. |
| `startedAt` | `string` | ISO 8601 deployment start. |
| `completedAt` | `string` | ISO 8601 deployment completion. |
| `startedBy` | `string` | Initiator identity. |
| `promotedFrom` | `string` | Source deployment ID if promoted. |

## Example

```bash title="cURL"
curl "/api/v1/deployments/deploy-003" \
  -H "Authorization: Bearer <accessToken>"
```

```json title="Response"
{
  "id": "deploy-003",
  "stackId": "stack-001",
  "stackName": "Analytics Platform",
  "version": 3,
  "environment": "PRODUCTION",
  "status": "SUCCEEDED",
  "steps": [
    {
      "layer": "ingestion",
      "resourceType": "pipeline",
      "resourceId": "pipe-001",
      "action": "UPDATE",
      "status": "SUCCEEDED",
      "startedAt": "2024-06-10T08:00:00Z",
      "completedAt": "2024-06-10T08:02:10Z",
      "logs": "Pipeline configuration updated. Restarted with new settings."
    },
    {
      "layer": "compute",
      "resourceType": "cluster",
      "resourceId": "cluster-001",
      "action": "NO_CHANGE",
      "status": "SKIPPED",
      "startedAt": null,
      "completedAt": null,
      "logs": "No changes detected."
    }
  ],
  "startedAt": "2024-06-10T08:00:00Z",
  "completedAt": "2024-06-10T08:05:33Z",
  "startedBy": "ada@datastack.dev",
  "promotedFrom": "deploy-002"
}
```
